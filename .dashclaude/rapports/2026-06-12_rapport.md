# Rapport de session — 2026-06-12

## Résumé

Session de déploiement et stabilisation v1.3.1. Test d'installation propre sur le PC Windows (192.168.1.44).

## Tâches complétées

### 1. Correction pipeline sync (sessions précédentes continuées)
- **INSERT IGNORE INTO** : corrigé pour gérer les doublons (MRK_ID = 0 dans ARTMARQUE)
- **Bug readline** : race condition `ERR_USE_AFTER_CLOSE` corrigée avec tracking des états `flushing`/`closed`
- **OOM sur ARTARTICLE (82K lignes)** : remplacé readline par `readFileSync` + split pour fichiers < 400MB
- **OOM sur CSHTICKETL (917MB)** : remplacé readline par lecteur chunk manuel (`fs.readSync`, 1MB chunks)
- **Filtre 4 ans** : ajout WHERE >= '2022-01-01' sur 8 tables transactionnelles (CSHTICKET, CSHTICKETL, NEGBL, NEGBLL, COMBCDE, COMBCDEL, RECBR, RECBRL)
- **Timezone** : toutes les dates passées en `Europe/Paris` au lieu d'UTC

### 2. Sync réussie
- 24 tables, 4 016 075 lignes, ~10 minutes
- Tables notables : CSHTICKETL (2 094 307 lignes), ARTCODEBARRE (703 651), AGRSTOCKCOUR (375 461)

### 3. Build & Release v1.3.1
- Version bumped à 1.3.1 partout (package.json racine, server-app/package.json, installer.iss)
- Build Electron avec `electron-builder --config electron-builder.server.yml`
- Correction installeur gonflé (700MB → 77MB) : `dist/win-unpacked/` inclus par erreur dans l'asar
- Tag v1.3.1, push GitHub, release créée

### 4. Nettoyage complet du PC Windows pour install propre
- Tué processus `_unins.tmp` (PID 1516) — ancien désinstalleur Inno Setup encore actif
- Supprimé dossiers : `C:\Ginkoyes`, `C:\Program Files\Ginkoyes Serveur`, AppData (Local + Roaming)
- Supprimé service Windows `ginkoyessync.exe`
- Supprimé base de données + utilisateur MariaDB
- Nettoyé registre (HKLM + HKCU) — entrée NSIS `fbdd69e1-e400-58d9-8d87-49a836a4add3`
- Supprimé anciens installeurs du bureau

## Problèmes rencontrés

1. **"Ginkoyes ne peut pas être fermé"** lors de l'install : causé par entrée registre NSIS stale (`HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall\fbdd69e1-...`) pointant vers un exe qui n'existait plus
2. **Fichier `unins000.dat` verrouillé** : processus `_unins.tmp` orphelin d'Inno Setup
3. **Heap OOM** même à 4GB : readline de Node.js fuit en mémoire avec mysql2 — résolu avec lecteur chunk manuel

## Prochaines étapes

1. PC en cours de redémarrage — relancer l'installeur `GinkoyesSetup131.exe`
2. Après install GUI : déployer fichiers sync (sync.js, service.js, sync-config.json)
3. Configurer MariaDB (database + user + tables)
4. Lancer première sync
5. Installer service Windows
6. Vérifier que l'interface Electron affiche les données correctement

## Versions

- **App** : v1.3.1
- **Installeur** : GinkoyesSetup131.exe (80MB, NSIS)
- **Node heap** : --max-old-space-size=4096

## Infra

- PC Windows : 192.168.1.44 (SSH user: edwar)
- MariaDB : localhost:3306
- Firebird : SV.GBK via gbak → isql CLI
