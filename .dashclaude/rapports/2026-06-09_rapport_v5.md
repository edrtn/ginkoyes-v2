# Rapport de session — 2026-06-09 (v5)

## Migration Electron + MariaDB + Tailscale

### Taches completees

#### Sprint 1 : Schema MariaDB + Script de sync
- `sql/001_schema.sql` — 24 tables miroir Firebird avec FK dans l'ordre des dependances
- `sql/002_indexes.sql` — Index optimises selon les patterns de requetes
- `sql/003_custom_tables.sql` — Tables custom (_sync_meta, _app_settings)
- `sync/sync.ts` — Script de sync nightly (GBK restore, Firebird read, MariaDB TRUNCATE+INSERT batch)
- `sync/sync-config.json` — Configuration chemins/credentials
- `sync/install-task.ps1` — Installation tache planifiee Windows (23h10)

#### Sprint 2 : Migration couche DB
- `lib/db.ts` — Reecrit : mysql2/promise pool, failover LAN→Tailscale, retry 3x
- `lib/config.ts` — Nouveau : gestion config runtime (env vars ou electron-store)
- `lib/queries.ts` — Toutes les requetes traduites Firebird→MySQL :
  - `SELECT FIRST N` → `LIMIT N` en fin de requete
  - `IIF()` → `IF()`
  - `EXTRACT(YEAR/MONTH FROM d)` → `YEAR(d)` / `MONTH(d)`
  - `? || '%'` → `CONCAT(?, '%')`
  - `INSERT ... RETURNING` → `INSERT` + `SELECT` par nom
- `app/api/articles/route.ts` — Ajoute `LIMIT 50`
- `app/api/stock/route.ts` — Ajoute `LIMIT 100`
- `app/api/achat/collections/route.ts` — Reecrit POST (INSERT puis SELECT)

#### Sprint 3 : Scaffolding Electron
- `electron/main.ts` — Entry point (BrowserWindow, IPC, menu)
- `electron/server.ts` — Custom server Next.js embarque
- `electron/preload.ts` — Bridge IPC (getDbConfig, setDbConfig, testDbConnection)
- `electron/store.ts` — Persistance config via electron-store
- `tsconfig.electron.json` — TypeScript config separee
- `electron-builder.yml` — Config packaging (NSIS Windows + DMG Mac)
- `package.json` — MAJ deps (mysql2, electron-store) + scripts electron

#### Sprint 4 : Page Parametres
- `app/settings/page.tsx` — Formulaire config DB avec test de connexion LAN/Tailscale
- `components/Navigation.tsx` — Ajoute lien Parametres (icone engrenage)
- `next.config.ts` — Ajoute `output: 'standalone'`
- `.gitignore` — Ajoute dist-electron, release

### Prochaines etapes (Sprint 5)
1. `npm install` pour installer les nouvelles dependances
2. Installer MariaDB et creer la base avec les fichiers SQL
3. Tester `next dev` contre MariaDB
4. Tester le build Electron (`npm run electron:build`)
5. Installer Tailscale + tester connectivite VPN
6. Configurer la tache planifiee sync sur le serveur Windows
7. QA final sur les 7 pages + settings
