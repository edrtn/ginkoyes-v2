# Rapport — 2026-06-17

## Taches completees

### SportLink Server v2.0.0
- **Renommage complet** Ginkoyes → SportLink dans 13 fichiers (sync, server-app, installer, scripts PS1)
- **Nouveau repertoire d'installation** : `C:\sportlink-serveur`
- **Copie GBK reseau integree** : nouvelle fonction `copyGbk()` dans `sync.ts` qui copie `\\SERVEUR\Backup\Save1\SV.GBK` → local avant restore
- **Montage automatique partage reseau** : `net use` avec credentials dans `copyGbk()` via config `network.share/user/password`
- **Retrocompatibilite** : si `gbkSourcePath` absent, utilise `gbkLocalPath` directement ; si ancien `gbkPath` present, le traite comme `gbkLocalPath`
- **Schedule par defaut** : 02:00 (etait 23:10)

### CI/CD
- **Workflow `build-server.yml`** : tags `server-v*` (separes des tags `v*` du dashboard pour ne pas casser l'auto-updater Electron)
- **Ajout etape** compilation sync TypeScript avant le build Electron dans le CI
- **Release `server-v2.0.0`** creee sur GitHub avec `SportLinkServer-Setup-2.0.0.exe`

### Deploiement PC SportLink (192.168.1.13)
- Installation via l'installeur InnoSetup
- Correction du code compile (sync.ts → dist/sync.js)
- Configuration du partage reseau persistant (`Z:` → `\\SERVEUR\Backup`)
- Service `SportLinkSync` installe et demarre
- **Sync complete reussie** : 24 tables, 2 274 477 lignes, 14 minutes
- Config avec credentials reseau deployee pour la sync automatique a 02:00

## Problemes rencontres

1. **dist/sync.js ancien** : le CI ne compilait pas le sync avant le build Electron → corrige dans le workflow
2. **Auto-updater casse** : le tag `v2.0.0` faisait chercher `latest-mac.yml` au dashboard → supprime et recree en `server-v2.0.0`
3. **Partage reseau inaccessible via SSH** : les sessions SSH n'heritent pas des lecteurs mappes ni des credentials Windows → resolu en ajoutant `net use` automatique dans le code `copyGbk()`
4. **Echappement `!` dans mot de passe** via SSH/bat → contourne avec PowerShell encodedcommand
5. **BOM UTF-8** dans le JSON ecrit par PowerShell → corrige

## Prochaines etapes

- Verifier que la sync automatique a 02:00 fonctionne (montage reseau + copie + restore + sync)
- Configurer le dashboard SportLink pour se connecter via Tailscale (IP: 100.120.238.108)
- Mettre a jour `setup.ps1` pour generer la section `network` dans la config
